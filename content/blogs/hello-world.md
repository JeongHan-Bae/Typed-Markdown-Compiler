---
title: Hello, static world
description: Why the first build should produce a complete site, not a browser-side promise.
date: "2026-08-08"
indexed: 1
tags:
  - architecture
  - static-sites
---

# Hello, static world

A content compiler earns its keep at build time. Once Markdown becomes HTML, a static host can serve the result with no application process and no client-side parser.

The result is portable: build once, then deploy the generated directory anywhere that serves files.
